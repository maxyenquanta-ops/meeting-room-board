# 會議室預約看板 — 需求規格 spec.md

## 定位

放在**一台共用電腦**上的單機看板／預約終端：同事走到這台電腦前，用瀏覽器查看
3 間會議室當日的預約狀況，並直接以「填名字」的方式預約。資料存在這台電腦的
localStorage，不做帳號、不做後端。

### 訪談結論摘要

| 項目 | 決定 |
|------|------|
| 使用形態 | 個人預約系統，實際部署在一台共用電腦上操作 |
| 規模 | 3 間會議室、單一地點、使用人數少 |
| 身分辨識 | 免登入，預約時填姓名即可 |
| 時間粒度 | 以小時為單位 |
| 營運時段 | 工作日（週一～週五）上班時間 |
| 預約欄位 | 預約人姓名（必填）、會議主題（必填） |
| 修改／取消 | 任何人都可以改／刪任何一筆 |
| 衝突規則 | 同一間會議室、同一時段不可重複預約 |
| 資料儲存 | 瀏覽器 localStorage（單機） |
| 部署 | 公司內網／內部主機，共用電腦上開瀏覽器使用 |
| 主畫面 | 單日檢視，3 間會議室並列 |
| 預約射程 | 不限制未來日期 |

---

## 1. 目標

- 讓同事在一個共用畫面上，一眼看出當日 3 間會議室誰在什麼時段有會。
- 讓預約動作簡單到「點一個時段、填名字和主題、送出」即可完成。
- 避免同一間會議室同一時段被兩個人重複預約。
- 零維運：不需要伺服器、資料庫、帳號管理。

## 2. 使用者

| 角色 | 說明 | 需求 |
|------|------|------|
| 一般同事 | 需要開會的人，走到共用電腦前操作 | 查看空檔、快速預約、找到並取消自己的預約 |
| 現場路過者 | 只想知道某會議室現在／等一下有沒有人 | 一眼看懂當日看板 |

> 沒有管理員角色。所有人權限相同。
>
> 訪談中回答「不要 / 未選取」的項目，全數列於第 4 節「非目標」。

## 3. 功能清單（MVP）

### F1 當日看板（主畫面）
- 3 間會議室並列為 3 欄，左側為時間軸（工作時間，整點分隔）。
- 顯示當日每個時段的狀態：空閒 / 已預約（顯示預約人姓名 + 會議主題）。
- 明顯標示「今天」與目前時間（例如一條現在時間線或高亮當前時段）。

### F2 切換日期
- 可往前 / 往後切換到其他工作日；提供「回到今天」。
- 週六、週日不顯示為可預約日（或顯示為休息）。

### F3 新增預約
- 點選某會議室的某個空閒整點時段開始預約。
- 表單欄位：會議室（帶入）、日期（帶入）、開始時間、結束時間、預約人姓名（必填）、會議主題（必填）。
- 時間以整點小時為單位，結束時間須晚於開始時間。
- 送出前檢查：該會議室該時段（含跨多小時的每一小時）皆無既有預約，否則擋下並提示衝突。

### F4 修改預約
- 點既有預約可編輯其姓名、主題、起訖時間。
- 修改時間時同樣做衝突檢查（排除自己）。

### F5 取消預約
- 點既有預約可刪除，需二次確認。
- 任何人皆可取消任何一筆。

### F6 本機資料持久化
- 所有預約資料存於 localStorage，重新整理／關掉瀏覽器再開仍在。
- 提供匯出 / 匯入 JSON（備份與換機用），以及「清除全部」。

## 4. 非目標（Out of Scope）

> 以下為訪談中明確「不要 / 未選取」的項目，全部列為非目標。

### 身分與權限
- 不做帳號、密碼、登入、權限分級。
- 不做驗證碼 / PIN 保護（改刪不需驗證）。
- 不做瀏覽器身分綁定（不以 localStorage 記住「這台電腦建立的預約」來限制只能改自己的）。
- 不做管理員 / 審核角色與審核流程。

### 資料與整合
- 不做後端伺服器、資料庫（SQLite / Postgres 等）。
- 不做 BaaS（Firebase / Supabase 等）。
- 不做跨裝置 / 跨瀏覽器的資料同步（無雲端）。
- 不整合 Google Calendar / Outlook / 公司行事曆。
- 不做 email / 通訊軟體通知或提醒。

### 預約規則
- 不做「單次最長時長」限制。
- 不做「可預約天數上限」限制（未來日期不設限）。
- 不做重複性預約（每週回議）。
- 不做報到 / 逾時自動釋放機制。
- 不阻擋預約已過去的時段（不特別做過去時間檢查）。

### 畫面與範圍
- 不做週檢視 / 多種檢視切換（只有單日、3 室並列）。
- 不做牆面被動顯示看板、門口平板預約機、報到機等其他使用情境。
- 不做「同一套系統同時支援多種情境」。
- 不做會議室屬性管理（容納人數、設備）與依條件搜尋。
- 不做多地點 / 多樓層階層。
- 不記錄預約人部門 / 分機、參加人數等欄位。
- 不做預約歷史統計、報表、稽核紀錄。
- 不做行動裝置專屬體驗（以共用電腦的桌面瀏覽器為主）。

## 5. 技術限制

- 純前端網頁，可用單一靜態頁面部署於公司內網主機，或直接以檔案開啟。
- 不可依賴外部網路服務（內網環境）。
- 資料層：localStorage，單機、單瀏覽器。
- 目標瀏覽器：共用電腦上的現代桌面瀏覽器（近版 Chrome / Edge 之一即可）。
- 時區：以該電腦本機時間為準，不處理跨時區。
- 資料模型（建議）：`booking = { id, roomId, date(YYYY-MM-DD), startHour, endHour, name, topic, createdAt }`；
  會議室清單為固定常數（3 間，可設定名稱）。

## 6. 待確認假設

- 上班時間預設 **09:00–18:00**（可調整常數）。
- 會議室名稱：預設「會議室 A / B / C」，實際名稱待提供。

## 7. 驗收標準

1. 開啟頁面即顯示今天、3 間會議室並列的當日看板，時間軸涵蓋 09:00–18:00。
2. 在空閒時段可完成一筆預約（填姓名 + 主題），送出後該時段立即顯示為已預約並顯示姓名與主題。
3. 對「同一會議室、同一時段」再次預約會被擋下並提示衝突；不同會議室的同一時段可各自預約。
4. 預約一筆跨 2 小時（如 14:00–16:00），該 2 個時段都被標記占用，且期間任一小時都無法再被預約。
5. 可將既有預約改到另一個空閒時段，原時段釋出、新時段占用；改到已被占用時段會被擋下。
6. 可取消任一筆預約（含他人建立的），取消需確認，取消後時段立即釋出。
7. 重新整理頁面後所有預約仍在；關閉瀏覽器再開仍在。
8. 可切換到明天 / 昨天等其他工作日並各自獨立顯示預約；「回到今天」可用；週末不開放預約。
9. 匯出 JSON 可下載目前所有預約；於同機匯入該檔可還原；「清除全部」後看板回到全空。
10. 全程無需登入、無網路請求；斷開內網 / 外網皆可正常操作。
